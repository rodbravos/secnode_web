---
import { Icon } from "astro-icon/components";

const links = [
    { href: '#inicio', text: 'Inicio' },
    { href: '#servicios', text: 'Servicios' },
    { href: '#soluciones', text: 'Soluciones' },
    { href: '#contacto', text: 'Contacto' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-brand-dark/80 backdrop-blur-md border-b border-brand-border/40 glow-effect">
    <nav class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
        <!-- LOGO -->
        <a href="/" class="flex items-center gap-3">
            <Icon name="logo-secnode" class="w-9 h-9" />
            
            <span class="text-xl font-bold tracking-wider text-white">
                <span class="text-brand-cyan">SEC</span>NODE
            </span>
        </a>

        <!-- Enlaces Desktop -->
        <div class="hidden md:flex items-center gap-8">
            {links.map((link) => (
                <a 
                    href={link.href} 
                    class="nav-link py-1 text-sm font-medium text-slate-300 hover:text-white transition-all border-b-2 border-transparent"
                >
                    {link.text}
                </a>
            ))}
        </div>

        <div class="flex items-center gap-4">
            <a href="#contacto" class="hidden sm:inline-flex px-5 py-2 rounded-lg text-sm font-semibold border border-brand-cyan/30 text-brand-cyan hover:bg-brand-cyan/10 transition-all shadow-lg shadow-cyan-400/10">
                Agenda una asesoría
            </a>

            <!-- Botón Hamburguesa (Visible en móviles) -->
            <button 
                id="menu-btn"
                aria-label="Toggle menu" 
                class="md:hidden p-2 text-slate-300 hover:text-white focus:outline-none flex flex-col justify-center items-center w-9 h-9 gap-[5px]"
            >
                <span class="w-6 h-[2px] bg-current rounded-full transition-all duration-300 ease-in-out bar-top"></span>
                <span class="w-6 h-[2px] bg-current rounded-full transition-all duration-300 ease-in-out bar-mid"></span>
                <span class="w-6 h-[2px] bg-current rounded-full transition-all duration-300 ease-in-out bar-bot"></span>
            </button>
        </div>
    </nav>

    <!-- Menú Desplegable Móvil -->
    <div id="mobile-menu" class="hidden md:hidden bg-brand-dark/95 border-b border-brand-border/40 px-6 py-6 space-y-4">
        {links.map((link) => (
            <a 
                href={link.href} 
                class="nav-link block text-base font-medium text-slate-300 hover:text-brand-cyan transition-colors"
            >
                {link.text}
            </a>
        ))}
        
        <!-- Botón CTA con estilo sólido y clase aislada para evitar conflictos con el JS -->
        <a 
            href="#contacto" 
            class="mobile-cta-btn block w-full text-center px-5 py-3 rounded-lg text-sm font-bold border border-brand-cyan/30 text-brand-cyan hover:bg-brand-cyan/10 transition-all mt-4 shadow-lg shadow-cyan-400/10"
        >
            Agenda una asesoría
        </a>
    </div>
</header>

<script>
    const initNavbar = () => {
        // Seleccionamos solo los enlaces de navegación directa (sin incluir el botón CTA)
        const navLinks = document.querySelectorAll<HTMLAnchorElement>('.nav-link');
        const sections = document.querySelectorAll<HTMLElement>('section[id]');
        
        const menuBtn = document.getElementById('menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');

        // Toggle Menú Móvil
        if (menuBtn && mobileMenu) {
            const toggleMenu = () => {
                menuBtn.classList.toggle('open');
                mobileMenu.classList.toggle('hidden');
            };

            menuBtn.onclick = toggleMenu;

            // Cierra el menú al hacer clic en cualquier enlace o botón dentro del menú móvil
            mobileMenu.querySelectorAll('a').forEach((link) => {
                link.addEventListener('click', () => {
                    if (!mobileMenu.classList.contains('hidden')) {
                        toggleMenu();
                    }
                });
            });
        }

        if (!navLinks.length || !sections.length) return;

        const activeClasses = ['text-white', 'border-brand-cyan', 'font-semibold'];
        const inactiveClasses = ['text-slate-300', 'border-transparent'];

        const setActive = (id: string) => {
            navLinks.forEach((link) => {
                const href = link.getAttribute('href');
                if (href === `#${id}`) {
                    link.classList.add(...activeClasses);
                    link.classList.remove(...inactiveClasses);
                } else {
                    link.classList.remove(...activeClasses);
                    link.classList.add(...inactiveClasses);
                }
            });
        };

        navLinks.forEach((link) => {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                const targetId = link.getAttribute('href');
                if (!targetId || targetId === '#') return;

                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    targetElement.scrollIntoView({ behavior: 'smooth' });
                    setActive(targetId.replace('#', ''));
                }
            });
        });

        const observerOptions = {
            root: null,
            rootMargin: '-20% 0px -65% 0px',
            threshold: 0
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    const id = entry.target.getAttribute('id');
                    if (id) setActive(id);
                }
            });
        }, observerOptions);

        sections.forEach((section) => observer.observe(section));
    };

    document.addEventListener('DOMContentLoaded', initNavbar);
    document.addEventListener('astro:page-load', initNavbar);
</script>

<style>
    #menu-btn.open .bar-top {
        transform: translateY(7px) rotate(45deg);
    }

    #menu-btn.open .bar-mid {
        opacity: 0;
        transform: translateX(-8px);
    }

    #menu-btn.open .bar-bot {
        transform: translateY(-7px) rotate(-45deg);
    }
</style>